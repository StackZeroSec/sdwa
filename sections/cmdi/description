A command injection is a type of attack where the attacker executes unintended commands on a system by exploiting flaws in the way the system processes user input. Command injection attacks are possible when an application passes unsafe user-supplied data (forms, cookies, HTTP headers, etc.) to a system shell for execution. Injection flaws can lead to data loss, corruption, or complete host takeove